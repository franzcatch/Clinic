(function (angular) {
    'use strict';

    angular.module('clinic')
        .controller('AppointmentCtrl', AppointmentCtrl);

    function AppointmentCtrl($scope) {
        
    }
} ((<any>window).angular));