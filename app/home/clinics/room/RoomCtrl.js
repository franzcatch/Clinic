(function (angular) {
    'use strict';
    angular.module('clinic')
        .controller('RoomCtrl', RoomCtrl);
    function RoomCtrl($scope, $uibModal, $uibModalInstance, params) {
    }
}(window.angular));
